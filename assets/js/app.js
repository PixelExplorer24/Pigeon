const firebaseConfig={apiKey:"AIzaSyDOU1dpWdqTZjGMjQjX_V3Iv6JMvcIoa2I",authDomain:"pigeon-7a637.firebaseapp.com",projectId:"pigeon-7a637",storageBucket:"pigeon-7a637.firebasestorage.app",messagingSenderId:"560041972635",appId:"1:560041972635:web:e9dda41d8abbccfda8473e",measurementId:"G-Q01M0H0TFK"};
firebase.initializeApp(firebaseConfig);const auth=firebase.auth(),db=firebase.firestore(),APP='pigeon-7a637';
const ROOT=()=>db.collection('artifacts').doc(APP).collection('public').doc('data');
const USERS=()=>ROOT().collection('users'), FRIENDS=()=>ROOT().collection('friends'), REQUESTS=()=>ROOT().collection('friendRequests'), MESSAGES=()=>ROOT().collection('messages');
const IMG_BB_KEY='1abc9f66636c45ace1d0952e080d153d';
let me=null,profile=null,users=[],friends=[],requests=[],activeFriend=null,chatUnsubs=[],listUnsubs=[],typingUnsub=null,typingTimer=null,attachedImages=[];
let messageMap=new Map();
const $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const avatar=u=>u?.photoURL||'https://placehold.co/120x120/e5e7eb/64748b?text=U';
const time=v=>{let n=v?.toMillis?v.toMillis():Number(v||0);if(!n)return 'now';let d=new Date(n),now=new Date();if(d.toDateString()===now.toDateString())return d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});return d.toLocaleDateString([],{day:'2-digit',month:'short'});};
function toast(t){const e=$('toast');e.textContent=t;e.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove('show'),2200)}
function pair(a,b){return [a,b].sort().join('__')}
function isFriend(uid){return friends.some(f=>f.friendUid===uid)}
function closeAllModals(){document.querySelectorAll('.modal').forEach(x=>x.classList.add('hidden'))}
function showView(id){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));$(id).classList.add('active');document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.view===id));}
function syncProfile(){if(!profile)return;const name=profile.displayName||me.displayName||me.email?.split('@')[0]||'User';$('headerAvatar').src=avatar(profile);$('profileAvatar').src=avatar(profile);$('profileName').textContent=name;$('profileEmail').textContent=profile.email||me.email||'';$('editName').value=name;$('editPhoto').value=profile.photoURL||'';$('editBio').value=profile.bio||'';}
async function ensureUser(){const ref=USERS().doc(me.uid);const snap=await ref.get();const base={uid:me.uid,displayName:me.displayName||me.email?.split('@')[0]||'User',email:me.email||'',photoURL:me.photoURL||null,lastSeen:firebase.firestore.FieldValue.serverTimestamp(),online:true};if(!snap.exists)await ref.set(base);else await ref.set({lastSeen:base.lastSeen,online:true}, {merge:true});profile={...base,...(snap.exists?snap.data():{})};syncProfile();}
function heartbeat(){if(!me)return;USERS().doc(me.uid).set({online:true,lastSeen:firebase.firestore.FieldValue.serverTimestamp()},{merge:true}).catch(()=>{});clearInterval(heartbeat.t);heartbeat.t=setInterval(()=>USERS().doc(me.uid).set({online:true,lastSeen:firebase.firestore.FieldValue.serverTimestamp()},{merge:true}).catch(()=>{}),30000)}
window.addEventListener('beforeunload',()=>{if(me)USERS().doc(me.uid).set({online:false,lastSeen:firebase.firestore.FieldValue.serverTimestamp()},{merge:true}).catch(()=>{})});
function startListeners(){stopListeners();listUnsubs.push(USERS().onSnapshot(s=>{users=s.docs.map(d=>({uid:d.id,...d.data()})).filter(x=>x.uid!==me.uid);renderPeople();renderChats();}));listUnsubs.push(FRIENDS().where('ownerUid','==',me.uid).onSnapshot(s=>{friends=s.docs.map(d=>({id:d.id,...d.data()}));renderPeople();renderChats();updateStats();}));listUnsubs.push(REQUESTS().where('receiverUid','==',me.uid).onSnapshot(s=>{requests=s.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.status==='pending');updateRequestBadge();renderPeople();}));listenChatIndex();}
function stopListeners(){listUnsubs.forEach(u=>u&&u());listUnsubs=[];closeChat();}
function listenChatIndex(){listUnsubs.push(MESSAGES().where('senderUid','==',me.uid).onSnapshot(()=>renderChats()));listUnsubs.push(MESSAGES().where('receiverUid','==',me.uid).onSnapshot(()=>renderChats()));}
async function getLatestMessages(){const [a,b]=await Promise.all([MESSAGES().where('senderUid','==',me.uid).get(),MESSAGES().where('receiverUid','==',me.uid).get()]);messageMap.clear();[...a.docs,...b.docs].forEach(d=>messageMap.set(d.id,{id:d.id,...d.data()}));return [...messageMap.values()].sort((a,b)=>(b.createdAt?.toMillis?.()||0)-(a.createdAt?.toMillis?.()||0));}
async function renderChats(){if(!me)return;const q=($('chatSearch')?.value||'').trim().toLowerCase();const all=await getLatestMessages();const by=new Map();for(const m of all){const uid=m.senderUid===me.uid?m.receiverUid:m.senderUid;if(!by.has(uid))by.set(uid,m)}let rows=[...by.entries()].map(([uid,m])=>({uid,m,u:users.find(x=>x.uid===uid)||friends.find(x=>x.friendUid===uid)||{uid,displayName:'User'}}));if(q)rows=rows.filter(r=>(r.u.displayName||'').toLowerCase().includes(q)||(r.u.email||'').toLowerCase().includes(q)||(r.m.text||'').toLowerCase().includes(q));const box=$('chatList');if(!rows.length){box.innerHTML='<div class="empty"><i class="fa-regular fa-comments" style="font-size:28px;display:block;margin-bottom:10px"></i>কোনো conversation নেই। People থেকে একজনকে বেছে নিয়ে chat শুরু করুন।</div>';return}box.innerHTML=rows.map(r=>`<button class="chat-item" onclick="openChat('${esc(r.uid)}')"><img class="avatar" src="${esc(avatar(r.u))}"><span class="item-copy"><strong>${esc(r.u.displayName||r.u.email||'User')}</strong><small>${esc(r.m.text||((r.m.imageUrls||[]).length?'📷 Image':'Message'))}</small></span><time class="item-meta">${time(r.m.createdAt)}</time></button>`).join('')}
let peopleTab='friends';function renderPeople(){const q=($('peopleSearch')?.value||'').trim().toLowerCase();let rows=[];if(peopleTab==='friends')rows=friends.map(f=>users.find(u=>u.uid===f.friendUid)||{uid:f.friendUid,displayName:'User'});else if(peopleTab==='requests')rows=requests.map(r=>users.find(u=>u.uid===r.senderUid)||{uid:r.senderUid,displayName:'User'});else rows=users;if(q)rows=rows.filter(u=>(u.displayName||'').toLowerCase().includes(q)||(u.email||'').toLowerCase().includes(q));const box=$('peopleList');if(!rows.length){box.innerHTML='<div class="empty">কোনো user পাওয়া যায়নি।</div>';return}box.innerHTML=rows.map(u=>{const pending=requests.find(r=>r.senderUid===u.uid);const f=isFriend(u.uid);let actions=f?`<button class="small-btn primary" onclick="openChat('${u.uid}')">Message</button>`:pending?`<button class="small-btn" disabled>Accept below</button>`:`<button class="small-btn primary" onclick="sendRequest('${u.uid}')">Add friend</button>`;if(peopleTab==='requests'&&pending)actions=`<button class="small-btn primary" onclick="acceptRequest('${pending.id}','${u.uid}')">Accept</button><button class="small-btn danger" onclick="rejectRequest('${pending.id}')">Decline</button>`;return `<div class="person-item"><img class="avatar" src="${esc(avatar(u))}"><div class="item-copy" onclick="openUser('${u.uid}')"><strong>${esc(u.displayName||'User')}</strong><small>${esc(u.email||'')}</small></div><div class="person-actions">${actions}</div></div>`}).join('')}
async function sendRequest(uid){if(!me||uid===me.uid||isFriend(uid))return;try{const existing=await REQUESTS().where('senderUid','==',me.uid).get();if(existing.docs.some(d=>d.data().receiverUid===uid&&d.data().status==='pending'))return toast('Request already sent');await REQUESTS().doc(pair(me.uid,uid)).set({senderUid:me.uid,receiverUid:uid,pairId:pair(me.uid,uid),status:'pending',createdAt:firebase.firestore.FieldValue.serverTimestamp()});toast('Friend request sent')}catch(e){console.error(e);toast('Request পাঠানো যায়নি')}}
async function acceptRequest(id,uid){try{const batch=db.batch();batch.update(REQUESTS().doc(id),{status:'accepted',respondedAt:firebase.firestore.FieldValue.serverTimestamp()});batch.set(FRIENDS().doc(pair(me.uid,uid)+'__'+me.uid),{pairId:pair(me.uid,uid),ownerUid:me.uid,friendUid:uid,createdAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});await batch.commit();toast('Friend added')}catch(e){console.error(e);toast('Accept করা যায়নি')}}
async function rejectRequest(id){try{await REQUESTS().doc(id).set({status:'rejected',respondedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});toast('Request declined')}catch(e){toast('কাজটি করা যায়নি')}}
function updateRequestBadge(){const n=requests.length;['requestBadge','navPeopleBadge'].forEach(id=>{const e=$(id);e.textContent=n;e.classList.toggle('hidden',!n)});}
async function updateStats(){if(!me)return;const msgs=await getLatestMessages();$('statChats').textContent=new Set(msgs.map(m=>m.senderUid===me.uid?m.receiverUid:m.senderUid)).size;$('statFriends').textContent=friends.length;$('statSent').textContent=msgs.filter(m=>m.senderUid===me.uid).length}
window.openUser=uid=>{const u=users.find(x=>x.uid===uid)||friends.find(x=>x.friendUid===uid);if(!u)return;$('userModalAvatar').src=avatar(u);$('userModalName').textContent=u.displayName||'User';$('userModalEmail').textContent=u.email||'';$('userModalBio').textContent=u.bio||'No bio added.';$('userModalChat').onclick=()=>{closeAllModals();openChat(uid)};$('userModal').classList.remove('hidden')};
function subscribeChat(uid){chatUnsubs.forEach(u=>u&&u());chatUnsubs=[];const render=()=>renderMessages();const ref=MESSAGES();chatUnsubs.push(ref.where('senderUid','==',me.uid).where('receiverUid','==',uid).onSnapshot(render));chatUnsubs.push(ref.where('senderUid','==',uid).where('receiverUid','==',me.uid).onSnapshot(render));}
async function renderMessages(){if(!activeFriend)return;const [a,b]=await Promise.all([MESSAGES().where('senderUid','==',me.uid).where('receiverUid','==',activeFriend.uid).get(),MESSAGES().where('senderUid','==',activeFriend.uid).where('receiverUid','==',me.uid).get()]);const arr=[...a.docs,...b.docs].map(d=>({id:d.id,...d.data()})).sort((x,y)=>(x.createdAt?.toMillis?.()||0)-(y.createdAt?.toMillis?.()||0));const box=$('messages');box.innerHTML=arr.length?arr.map(m=>{const mine=m.senderUid===me.uid;const imgs=m.imageUrls||[];return `<div class="msg-row ${mine?'mine':'theirs'}"><div class="bubble">${m.text?`<div>${esc(m.text).replace(/\n/g,'<br>')}</div>`:''}${imgs.map(u=>`<img class="msg-img" src="${esc(u)}" onclick="window.open(this.src,'_blank')">`).join('')}<div class="msg-time">${time(m.createdAt)} ${mine?'✓':''}</div></div></div>`}).join(''):'<div class="empty" style="margin-top:30px">এখানে আপনার private conversation শুরু হবে।</div>';box.scrollTop=box.scrollHeight}
async function openChat(uid){let u=users.find(x=>x.uid===uid)||friends.find(x=>x.friendUid===uid);if(!u)return;activeFriend=u;$('chatName').textContent=u.displayName||'User';$('chatAvatar').src=avatar(u);$('chatStatus').textContent=u.online?'online':`last seen ${time(u.lastSeen)}`;$('chatPanel').classList.remove('hidden');document.body.style.overflow='hidden';subscribeChat(uid);await renderMessages()}
function closeChat(){chatUnsubs.forEach(u=>u&&u());chatUnsubs=[];if(typingUnsub)typingUnsub();typingUnsub=null;activeFriend=null;$('chatPanel')?.classList.add('hidden');document.body.style.overflow=''}
async function uploadImage(file){const fd=new FormData();fd.append('image',file);const r=await fetch(`https://api.imgbb.com/1/upload?key=${IMG_BB_KEY}`,{method:'POST',body:fd});const j=await r.json();if(!j.success)throw new Error('ImgBB upload failed');return j.data.url}
async function sendMessage(e){e.preventDefault();if(!activeFriend||!me)return;const input=$('messageInput'),text=input.value.trim();if(!text&&!attachedImages.length)return;const btn=document.querySelector('.send-btn');btn.disabled=true;try{const imageUrls=[];for(const f of attachedImages)imageUrls.push(await uploadImage(f));await MESSAGES().add({senderUid:me.uid,receiverUid:activeFriend.uid,text,imageUrls,imageUrl:imageUrls[0]||'',createdAt:firebase.firestore.FieldValue.serverTimestamp(),seen:false});input.value='';attachedImages=[];renderAttachmentPreview();toast('Message sent')}catch(e){console.error(e);toast('Message পাঠানো যায়নি')}finally{btn.disabled=false;input.focus()}}
function renderAttachmentPreview(){const b=$('attachmentPreview');b.classList.toggle('hidden',!attachedImages.length);b.innerHTML=attachedImages.map((f,i)=>`<img src="${URL.createObjectURL(f)}" title="${esc(f.name)}" onclick="attachedImages.splice(${i},1);renderAttachmentPreview()">`).join('')}
function handleTyping(){if(!activeFriend)return;clearTimeout(typingTimer);USERS().doc(me.uid).set({typingTo:activeFriend.uid,typingAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true}).catch(()=>{});typingTimer=setTimeout(()=>USERS().doc(me.uid).set({typingTo:null},{merge:true}).catch(()=>{}),1200)}
function watchTyping(){if(!activeFriend)return;typingUnsub=USERS().doc(activeFriend.uid).onSnapshot(s=>{const d=s.data()||{},on=d.typingTo===me.uid;$('typing').classList.toggle('hidden',!on)})}
async function saveProfile(){const name=$('editName').value.trim();if(!name)return;try{await USERS().doc(me.uid).set({displayName:name,photoURL:$('editPhoto').value.trim()||null,bio:$('editBio').value.trim(),profileUpdatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});profile={...profile,displayName:name,photoURL:$('editPhoto').value.trim()||null,bio:$('editBio').value.trim()};syncProfile();closeAllModals();toast('Profile updated')}catch(e){toast('Profile save হয়নি')}}
$('googleLogin').onclick=async()=>{const b=$('googleLogin');b.disabled=true;try{await auth.signInWithPopup(new firebase.auth.GoogleAuthProvider())}catch(e){console.error(e);$('loginError').textContent=e.message||'Login failed'}finally{b.disabled=false}};
auth.onAuthStateChanged(async user=>{if(user){me=user;$('loginScreen').classList.add('hidden');$('app').classList.remove('hidden');await ensureUser();heartbeat();startListeners();syncProfile();}else{$('app').classList.add('hidden');$('loginScreen').classList.remove('hidden')}});
document.querySelectorAll('.nav-item').forEach(b=>b.onclick=()=>showView(b.dataset.view));document.querySelectorAll('[data-people-tab]').forEach(b=>b.onclick=()=>{peopleTab=b.dataset.peopleTab;document.querySelectorAll('[data-people-tab]').forEach(x=>x.classList.toggle('active',x===b));renderPeople()});$('peopleSearch').oninput=renderPeople;$('chatSearch').oninput=()=>renderChats();$('refreshBtn').onclick=()=>{renderChats();renderPeople();toast('Refreshed')};$('newChatBtn').onclick=()=>{showView('peopleView');peopleTab='all';document.querySelectorAll('[data-people-tab]').forEach(x=>x.classList.toggle('active',x.dataset.peopleTab==='all'));renderPeople();$('peopleSearch').focus()};$('backChat').onclick=closeChat;$('composer').onsubmit=sendMessage;$('messageInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(e.target.scrollHeight,120)+'px';handleTyping()});$('imageInput').onchange=e=>{attachedImages=[...attachedImages,...Array.from(e.target.files||[]).filter(f=>f.type.startsWith('image/'))].slice(0,5);e.target.value='';renderAttachmentPreview()};$('editProfileBtn').onclick=()=>{$('profileModal').classList.remove('hidden')};$('profileBtn').onclick=()=>showView('profileView');$('saveProfile').onclick=saveProfile;$('logoutBtn').onclick=async()=>{if(confirm('Log out করবেন?')){if(me)await USERS().doc(me.uid).set({online:false,lastSeen:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});await auth.signOut()}};document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>$(b.dataset.close).classList.add('hidden'));$('chatInfo').onclick=()=>{if(activeFriend)openUser(activeFriend.uid)};
const observer=new MutationObserver(()=>{if(!$('chatPanel').classList.contains('hidden')&&activeFriend&&!typingUnsub)watchTyping()});observer.observe($('chatPanel'),{attributes:true,attributeFilter:['class']});
